import React from 'react';
import './css/header.css';

const header = () => {
    return (
        <header>
        <h1>Elden Ring Build Selector</h1>
        </header>
    );
};

export default header;